# IoT Scanner - Система безопасности IoT устройств

Полнофункциональное fullstack приложение для сканирования IoT устройств в сети и обнаружения уязвимостей безопасности.

## 🚀 Технологии

### Frontend

- **Next.js 14** (App Router)
- **TypeScript**
- **TailwindCSS**
- **ShadCN UI** компоненты
- **React 18**

### Backend

- **FastAPI** - REST API
- **SQLAlchemy** (async ORM)
- **PostgreSQL** - база данных
- **Go Scanner** - нативный сканер сети

### Сканер (Go)

- **Nmap** - сканирование портов
- **NetBIOS** (nmblookup) - определение hostname Windows/IoT устройств
- **mDNS** - определение hostname Apple/Linux устройств
- **ARP** - обнаружение устройств в сети

### Инфраструктура

- **Docker** & **Docker Compose**
- **PostgreSQL 15**

## 📋 Возможности

- 🔍 **Сканирование сети** - автоматическое обнаружение IoT устройств в указанной подсети
- 🛡️ **Анализ уязвимостей** - проверка безопасности устройств по открытым портам
- 📊 **Детальная информация** - IP, MAC, hostname, vendor, открытые порты
- 💡 **Рекомендации** - инструкции по устранению уязвимостей
- 📱 **Современный UI** - адаптивный интерфейс на React/Next.js
- 🚀 **Быстрый Go сканер** - нативная производительность, работает на хосте

## 🏗️ Структура проекта

```text
IoT-scanner/
├── docker-compose.yml
├── .env
├── backend/
│   ├── scanner/           # Go сканер (запускается на хосте)
│   │   ├── go.mod
│   │   ├── main.go
│   │   └── scanner        # скомпилированный бинарник
│   ├── routers/           # FastAPI роутеры
│   ├── services/          # Бизнес-логика
│   ├── models/            # SQLAlchemy модели
│   ├── schemas/           # Pydantic схемы
│   ├── repositories/      # Слой работы с БД
│   ├── core/              # Конфигурация, подключение к БД
│   └── main.py
└── frontend/
    ├── app/               # Next.js App Router страницы
    ├── components/        # React компоненты
    ├── hooks/             # Кастомные хуки
    ├── lib/               # API клиент, утилиты
    └── types/             # TypeScript типы
```

## 🏃 Быстрый старт

### Требования

- Docker и Docker Compose
- Go 1.21+ (для сборки сканера)
- nmap (устанавливается автоматически)
- nmblookup (опционально, для NetBIOS): `brew install samba`

### 1. Запуск инфраструктуры

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd IoT-scanner

# Создайте .env файл
cat > .env << EOF
POSTGRES_USER=iot_user
POSTGRES_PASSWORD=iot_password
POSTGRES_DB=iot_scanner
POSTGRES_HOST=127.0.0.1
DATABASE_URL=postgresql+asyncpg://iot_user:iot_password@127.0.0.1:5432/iot_scanner
EOF

# Запустите Docker контейнеры
docker compose up -d --build
```

### 2. Сборка Go сканера

```bash
cd backend/scanner
go mod tidy
go build -o scanner
```

### 3. Запуск сканирования

```bash
# Требуются права sudo для nmap
cd backend/scanner
sudo ./scanner -network="192.168.1.0/24"
```

### 4. Открыть приложение

- **Frontend**: <http://localhost:3000>
- **Backend API**: <http://localhost:8000>
- **API документация**: <http://localhost:8000/docs>

## 📖 Использование

### Запуск сканирования

1. Запустите Go сканер в терминале:

   ```bash
   sudo ./backend/scanner/scanner -network="192.168.1.0/24"
   ```

2. Сканер покажет прогресс:

   ```text
   ╔══════════════════════════════════════════════════════════════╗
   ║           IoT Scanner - Go Native Edition v1.0               ║
   ╚══════════════════════════════════════════════════════════════╝

   📡 Target network: 192.168.1.0/24
   📝 Creating scan in database...
   ✅ Created scan ID: 1

   🔍 Phase 1/3: ARP scan for live hosts...
   🔍 Phase 2/3: Nmap port scan...
   🔍 Phase 3/3: Resolving hostnames...

   📊 Total devices found: 13
   🎉 Scan completed!
   ```

3. Откройте <http://localhost:3000> для просмотра результатов

### Просмотр устройств

- Перейдите в раздел "Устройства" для списка обнаруженных устройств
- Кликните на устройство для детальной информации:
  - IP, MAC адрес, hostname, vendor
  - Список открытых портов
  - Обнаруженные уязвимости

### Параметры сканера

```bash
./scanner [flags]

Flags:
  -network string   Целевая сеть в CIDR формате (default "192.168.1.0/24")
  -api string       URL бэкенда (default "http://localhost:8000/api")
  -name string      Название сканирования
```

## 🔍 Методы определения hostname

Go сканер использует 3 метода для определения имён устройств:

1. **Reverse DNS** - стандартный DNS PTR запрос
2. **NetBIOS** (nmblookup) - работает для Windows, Keenetic роутеров, NAS
3. **mDNS** (dns-sd) - работает для macOS, Linux, принтеров

## 🔒 Обнаруживаемые порты

Сканер проверяет популярные порты IoT устройств:

| Порт | Сервис | Описание |
| ------ | -------- | ---------- |
| 21 | FTP | Файловый сервер |
| 22 | SSH | Secure Shell |
| 23 | Telnet | Небезопасный доступ |
| 80, 8080 | HTTP | Веб-интерфейс |
| 443, 8443 | HTTPS | Защищённый веб |
| 554, 8554 | RTSP | IP камеры |
| 1883 | MQTT | IoT протокол |
| 5432 | PostgreSQL | База данных |
| 3389 | RDP | Удалённый рабочий стол |

## ⚠️ Важные замечания

1. **Права доступа**: Сканер требует `sudo` для nmap SYN сканирования

2. **macOS + Docker**: На macOS Docker Desktop использует Linux VM, поэтому сканер должен запускаться на хосте (не в контейнере)

3. **Только своя сеть**: Сканируйте только сети, на которые у вас есть разрешение

4. **Время сканирования**: Полное сканирование /24 сети занимает 3-7 минут

## 🐛 Решение проблем

### Сканер не видит устройства

- Убедитесь что запускаете с `sudo`
- Проверьте что указали правильную подсеть
- Установите nmap: `brew install nmap`

### Hostname не определяются

- Установите samba для NetBIOS: `brew install samba`
- Некоторые устройства не поддерживают reverse DNS

### Backend не запускается

```bash
docker logs iot-scanner-backend
```

### Frontend ошибки

```bash
docker logs iot-scanner-frontend
docker compose up -d --build frontend
```

## 📝 API Endpoints

| Метод | Endpoint                   | Описание                          |
| ----- | -------------------------- | --------------------------------- |
| GET   | `/api/devices/`            | Список устройств                  |
| GET   | `/api/devices/{id}`        | Детали устройства                 |
| POST  | `/api/scans/`              | Создать скан                      |
| GET   | `/api/scans/`              | Список сканов                     |
| POST  | `/api/scans/{id}/results`  | Отправить результат (для сканера) |
| POST  | `/api/scans/{id}/complete` | Завершить скан                    |

Полная документация: <http://localhost:8000/docs>

## 📄 Лицензия

MIT License. Создано в образовательных целях для проекта по информационной безопасности.
